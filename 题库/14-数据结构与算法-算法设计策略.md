# 14 · 数据结构与算法：算法设计策略 · 题库

> 真题：消防栓贪心四问 2018下；递归式复杂度两问、0-1 与部分背包对比四问 年份待核实；策略辨析 2021上/2022下｜第二、三节有计算，阶段B｜未讲（自学讲义）
> 知识点范围：对齐 `外部资源/转录md/10-算法分析与设计.md` 全篇（算法特性、复杂度与渐进符号、递归、分治／动规／贪心／回溯／分支限界／概率／近似、数据挖掘与智能优化）
> 讲义 → 讲义/14-数据结构与算法-算法设计策略（√）.md

## 考试怎么考

**考点① 给一段描述认策略。** 题干里必有一个标志性短语，对上就完：出现"互不重叠／相互独立／分别求解后**合并**"→分治；"**重叠**子问题／避免重复计算／**存表填表**／自底向上"→动规；"每一步**局部最优**／一旦做出选择**不再改变**／按某种顺序依次选取"→贪心；"**试探**／走不通就**退回**／求**所有**满足条件的解／深度优先"→回溯；"求**最优解**／**界限函数**／提前判断没希望就不再展开／**优先队列**扩展"→分支限界。

**考点② "XX 与 YY 的主要区别是""以下叙述中不正确的是"。** 四个选项里三个抄定义、一个反着写，错项只有三种造法，认出造法就不必逐字比对。一是**对调**：把回溯写成求最优解、把分支限界写成求所有解，把"自底向上填表"安到分治头上、把"自顶向下递归"安到动规头上。二是**把不是分水岭的当分水岭**：拿"要不要最优子结构"来分动规和贪心（两边都要）、拿"是不是递归实现"或"有没有分解子问题"来分分治和动规（两边都有）。三是**把量级吹上去**："分支限界能把复杂度降到多项式级""贪心一定能得到最优解""动规一定比分治快""贪心能求出全部最优解"，全错。三对策略真正的分水岭只有三条，做题时对着它排除即可：

- 分治／动规：看子问题**重不重叠**（重叠的是动规）
- 动规／贪心：看有没有**贪心选择性质**（最优子结构两边都有，不是分水岭）
- 回溯／分支限界：看**目标**（所有解 vs 最优解）＋**搜索方式**（深度优先 vs 广度优先／最小耗费优先）

**考点③ 数字题。** 三种问法：把几个档位排序（唯一真会错的地方是 nlog₂n 和 n² 谁在前）；给一个递归式问复杂度（按第三节的树数法，别只盯着式子里的 f(n)、漏掉子问题个数带来的放大）；给一个具体算法问复杂度（先分清瓶颈在哪一步——部分背包贵在那次排序，不在装包那一遍）。要背的就这几个数：**0-1 背包动规 Θ(nW)**；**部分背包贪心 Θ(nlog₂n)**（瓶颈是排序）；**回溯与分支限界最坏仍 O(2ⁿ)、O(n!)**；界限方向——**求最大估上界、求最小估下界**。

---

## 练习

**1.【真题·年份待核实】** 已知算法 A 的运行时间函数为 T(n)=8T(n/2)+n^2，其中 n 表示问题的规模，则该算法的时间复杂度为（ ）另已知算法 B 的运行时间函数为 T(n)=XT(n/4)+n^2，其中 n 表示问题的规模。对充分大的 n，若要算法 B 比算法 A 快，则 X 的最大值为（ ）。
　　(62) A. Θ(n)　B. Θ(nlgn)　C. Θ(n²)　D. Θ(n³)
　　　　　A. 15　B.17　C. 63　D. 65
　　（录自培训资料转录本，未查到具体考试场次；"B.17"缺空格、"n^2"未写成上标，均为原件如此。）

**2.【真题·年份待核实】** 考虑一个背包问题，共有 n=5 个物品，背包容量为 W=10，物品的重量和价值分别为：w={2, 2, 6, 5, 4}，v={6,3, 5, 4, 6}，求背包问题的最大装包价值。若此为 0-1 背包问题，分析该问题具有最优子结构，定义递归式为其中 c（i，j）表示 i 个物品、容量为 j 的 0-1 背包问题的最大装包价值，最终要求解 c（n,W）。采用自底向上的动态规划方法求解，得到最大装包价值为（62），算法的时间复杂度为（63）。若此为部分背包问题，首先采用归并排序算法，根据物品的单位重量价值从大到小排序，然后依次将物品放入背包直至所有物品放入背包中或者背包再无容量，则得到的最大装包价值为（64），算法的时间复杂度（65）。
　　（62）A. 11　B. 14　C. 15　D. 16.67
　　（63）A. Θ(nW)　B. Θ(nlgn)　C. Θ(n)　D. Θ(nlgnW)
　　（64）A. 11　B. 14　C. 15　D. 16.67
　　（65）A. Θ(nW)　B. Θ(nlgn)　C. Θ(n)　D. Θ(nlgnW)
　　（录自培训资料转录本，未查到具体考试场次；"定义递归式为"后原件即缺公式，"v={6,3, 5, 4, 6}"的空格照录。希赛题库同题的（63）（65）C 选项印作 Θ(n²)，不影响答案。）

**3.【真题·2018下】** 在一条笔直公路的一边有许多房子，现要安装消防栓，每个消防栓的覆盖范围远大于房子的面积，如下图所示。现要求解能覆盖所有房子的最少消防栓数和安装方案（问题求解过程中，可将房子和消防栓视为直线上的点）。该问题求解算法的基本思路为：从左端的第一栋房子开始，在其右侧 m 米处安装一个消防栓，去掉被该消防栓覆盖的所有房子。在剩余的房子中重复上述操作，直到所有房子被覆盖。算法采用的设计策略为（ ）；对应的时间复杂度为（ ）。假设公路起点 A 的坐标为 0，消防栓的覆盖范围（半径）为 20 米，10 栋房子的坐标为（10，20，，30，35，60，80，160，210，260，300），单位为米。根据上述算法，共需要安装（ ）个消防栓。以下关于该求解算法的叙述中，正确的是（ ）。
　　（62）A. 分治　B. 动态规划　C. 贪心　D. 回溯
　　（63）A. O(lgn)　B. O(n)　C. O(nlgn)　D. O(n2)
　　（64）A. 4　B. 5　C. 6　D. 7
　　（65）A. 肯定可以求得问题的一个最优解　B. 可以求得问题的所有最优解　C. 对有些实例，可能得不到最优解　D. 只能得到近似最优解
　　（录自培训资料转录本。对照信管网《2018 年下半年软件设计师上午综合知识真题文字版》同题：坐标串里"20，，30"的重复逗号原卷即如此；（63）选项原卷印作 Θ(lgn)/Θ(n)/Θ(nlgn)/Θ(n²)，此处照录转录本的 O(·)，含义相同；原卷作"现求解……可将房子和消防栓均视为直线上的点"，转录本作"现要求解……视为"。）

**4.【模拟·重现2021上考点】** 求解某问题时，将其分解为若干子问题，这些子问题相互独立、互不重叠，分别求解后再将子问题的解合并得到原问题的解，该方法属于（ ）。A. 分治法　B. 动态规划法　C. 贪心法　D. 回溯法

**5.【模拟·重现2022下考点】** 某算法在求解每一步时只依据当前状态做出局部最优的选择，一旦做出选择就不再回溯或改变，该算法体现的设计策略是（ ）。A. 贪心法　B. 回溯法　C. 分支限界法　D. 动态规划法

**6.【模拟】** 动态规划法与分治法的主要区别是（ ）。
A. 动态规划法要把问题分解成子问题，分治法不分解子问题
B. 动态规划法的子问题相互重叠，分治法的子问题相互独立
C. 动态规划法自顶向下求解，分治法自底向上求解
D. 分治法只能求最优解，动态规划法可以求所有解

**7.【模拟】** 以下关于回溯法与分支限界法的叙述中，不正确的是（ ）。
A. 回溯法以深度优先方式搜索解空间树，分支限界法以广度优先或最小耗费优先方式搜索
B. 回溯法常用于求出满足约束条件的所有解，分支限界法常用于求最优解
C. 在分支限界法中，每个活结点只有一次机会成为扩展结点
D. 分支限界法通过剪枝可将问题的时间复杂度降低到多项式阶

**8.【模拟】** 用分支限界法求解 0-1 背包问题（求最大价值）时，对每个活结点估算的界限应为（ ）；若改为求解 TSP 最短回路，则应估算（ ）。A. 上界／上界　B. 上界／下界　C. 下界／上界　D. 下界／下界

**自测**：
9.【模拟】某算法用最少数量的硬币凑出目标金额，每一步都选不超过剩余金额的最大面额。这是哪种策略？在面额 {1, 3, 4} 凑 6 元时，它给出几枚？最优是几枚？
10.【模拟】数据挖掘里，"根据带类别标签的历史数据训练模型再预测新数据的类别"和"事先没有标签、按相似度把对象归堆"分别叫什么？各属有监督还是无监督？
11.【模拟】把下列算法归到五种策略：归并排序、Dijkstra、n 皇后、0-1 背包填表求解、TSP 加界限函数的搜索。

### 参考答案

**1. D、C**。第一问：n^(log₂8) = n³ 与 f(n) = n² 取大者，得 **Θ(n³)**（推导见第三节那棵递归树）。选 A/B/C 的都是只看见式子里的 n²、漏了 8 个子问题带来的放大。第二问：算法 B 的主导项是 n^(log₄X)，要小于 n³ 就要 X < 4³ = 64，整数最大 **63**。注意本题是按**量级**比快慢：X=64 时 B 也是 Θ(n³)，和 A 同档，不算"更快"；选项 D 的 65 已经越过 64，B 反而更慢，所以不选 D。

**2. C、A、D、B**。（62）0-1 背包：穷举与填表双路互验，c[5,10] = **15**（拿第 1、2、5 件，重 2+2+4=8 ≤ 10，价值 6+3+6=15）。（63）填 (n+1)×(W+1) 张表、每格一次比较，**Θ(nW)**。（64）部分背包可拆零：单位重量价值为 3、1.5、0.833、0.8、1.5，从高到低是 1、2、5、3、4；全取第 1、2、5 件用掉 8 斤得价值 15，还剩 2 斤，第 3 件（重 6、值 5）取 1/3 得 5/3，合计 15 + 5/3 = **16.67**。它比 0-1 版本的 15 高，正是"能拆零就不会更差"。（65）这一问考的不是装包本身，而是**这个算法的瓶颈在哪**：装包那一遍只是扫一趟 O(n)，真正贵的是题干指定的**归并排序**，**Θ(nlgn)**。

**3. C、B、B、（第四问两种口径）**。（62）"从左端第一栋开始装一个、去掉被覆盖的、在剩下的里重复"——每一步只看当下、装了不再挪，是**贪心**。（63）房子已按坐标给出，算法只从左到右扫一遍，**O(n)**。（64）半径 20 米，在第一栋房子右侧 20 米处装，覆盖区间就是 [该房, 该房+40]：栓在 30 覆盖 10/20/30/35；剩下最左是 60，栓在 80 覆盖 60/80；剩下 160，栓在 180 覆盖 160；剩下 210，栓在 230 覆盖 210；剩下 260，栓在 280 覆盖 260/300。共 **5** 个。（65）**主流参考答案分两派，不要当成已定论**：信管网真题库给 **A**（肯定可以求得一个最优解），希赛系题库与本课培训资料给 **C**（对有些实例可能得不到最优解）。分歧在问的是谁——题干这个具体做法"在最左边还没被覆盖的房子右侧恰好一个半径处放桩"，对"直线上用最少的点覆盖所有点"这类问题可以证明总能拿到最少桩数，按这个读法选 A；C 说的则是贪心法作为一类策略的通性。**这一问不是可靠得分点**，稳稳能排除的只有 B、D：B 错在"所有最优解"（算法只给一个方案），D 错在"只能得到近似最优解"（那是近似算法的说法）。同类题问"贪心法"就答"不保证全局最优"，问"该算法"则要看这个算法自己站不站得住。

**4. A**。"相互独立、互不重叠 + 分别求解 + 合并"是分治的定义原文；写明"互不重叠"就排除了动规。

**5. A**。"局部最优 + 一旦选择不再回溯或改变"是贪心的定义原文。回溯恰恰相反（会退回改选），排除 B。

**6. B**。A 错：两者都要把问题分解成子问题，"分不分解"不是区别点，区别在分出来的子问题彼此**独立还是重叠**。C **说反了**：动规通常自底向上填表，分治是自顶向下递归分解再合并。D 两头都错：分治不是专为求最优解的（归并排序就没有"最优"可言），求所有解通常是回溯的目标。

**7. D**。剪枝只是期望上更快，最坏仍是指数级——这是"错法三：把量级吹上去"的标准样子。A、B、C 三条都是培训资料原文。

**8. B**。求最大值估上界（上界 ≤ 当前最优就砍），求最小值估下界（下界 ≥ 当前最优就砍），本质都是"这支最好也就这样了"。

**9.** 贪心法。贪心先拿 4，剩 2 只能拿 1、1，共 **3 枚**；最优是 3+3，只要 **2 枚**（填表逐个金额推一遍即可验证）。这就是面额组合不具备贪心选择性质时贪心失手的样子——和课上那个 23 对 24 是同一回事。

**10.** 前者是**分类**，属**有监督**学习（要带标签的训练数据集）；后者是**聚类**，属**无监督**学习（没有标签，只按数据特征分堆）。

**11.** 归并排序＝分治（分解／求解／合并三步齐全）；Dijkstra＝贪心（每轮锁定当前距离最小的顶点，锁定后不改）；n 皇后＝回溯（逐行试探、冲突就撤销上一步重选）；0-1 背包填表＝动态规划（重叠子问题 + 自底向上填表）；TSP 加界限函数的搜索＝分支限界（求最优解 + 提前砍掉没希望的支）。
