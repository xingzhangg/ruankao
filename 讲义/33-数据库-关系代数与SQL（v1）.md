# 33 · 数据库：关系代数与 SQL

> 真题：自然连接属性列数与等价表达式、GROUP BY 分组聚合（培训资料四道原题，年份待核实）；关系代数↔SQL 互译 2021上、连接列数 2022下、GRANT/REVOKE 2024上｜有演算，阶段B｜未讲（自学讲义）
> 知识点范围：对齐 `外部资源/转录md/04-数据库技术基础.md` 第 5 节「关系代数」与「数据库新技术 6. SQL 语言」；外连接、除运算、子查询、视图与索引取舍、DDL/DML/DCL 分类、GRANT/REVOKE 按教材（第 5 版）补齐

---

## 定位

**这一课回答**：表建好了怎么查——关系代数六种运算怎么算行列数，SQL 的执行顺序怎么解释所有语法规则，GRANT/REVOKE 怎么管权限。

**前后关系**：30 讲表从哪来、31/32 讲表拆得对不对，本课讲怎么查；下午题 2 后半段的 SQL 填空全在本课。

**分值与去向**：数据库板块上午约 5–6 分，本课上午常年 1–2 分（自然连接列数、代数↔SQL 互译、GRANT/REVOKE 轮流出），下午题 2 还能占好几分，是板块内性价比最高的一课。要演算，阶段B。

---

## 开讲：你会写 SQL，只是从来没数过行和列

全课只用下面三张小表，所有运算和 SQL 都在它们身上做，行数列数你都能自己数。

```
S（学生）4行3列   C（课程）3行2列   SC（选课）5行3列
学号 姓名 年龄    课程号 课程名     学号 课程号 成绩
S01  张三  20     C01   数据库      S01  C01   85
S02  李四  22     C02   操作系统    S01  C02   70
S03  王五  20     C03   英语        S02  C01   90
S04  赵六  19                       S02  C02   60
                                    S03  C01   55
```

留意两处空缺：**赵六一门课没选，英语一个人没选**——第四节讲外连接全靠它俩。

先把词对上。数据库里的一张表，理论里叫**关系**；一行叫**元组**（一条记录），一列叫**属性**（一个字段），某属性允许取值的范围叫**域**（"年龄"的域是 0–150 的整数）；只写"表叫什么、有哪些列"的骨架叫**关系模式**，写作 S(学号, 姓名, 年龄)。

**关系代数**就是一组作用在表上的运算，核心设定只有一条：**输入是表，输出还是表**，所以能像四则运算那样层层嵌套。而你天天写的 SQL 是**声明式**的——只说"要什么"不说"怎么算"，数据库拿到它正是翻译成一串关系代数动作去执行。所以这一课不是学新东西，是**把已经会写的 SQL 拆回底下那几个动作**；拆开之后，"为什么 WHERE 里不能写 AVG()""为什么自然连接的列数要减一下"就都不用背了。

### 一、一张表只能两个方向切：选择 σ 与投影 π

**选择 σ（读 sigma）＝ 横着切、挑行 ＝ SQL 的 WHERE。** σ<sub>条件</sub>(R) 把满足条件的元组留下，**一列不动**。

σ<sub>年龄=20</sub>(S) 逐行验：S01 张三 20 ✓、S02 李四 22 ✗、S03 王五 20 ✓、S04 赵六 19 ✗，结果 **2 行 3 列**，对应 `SELECT * FROM S WHERE 年龄=20`。

**投影 π（读 pai）＝ 竖着切、挑列 ＝ SQL 里 SELECT 后面那串列名。** π<sub>列名</sub>(R) 只留指定的列，一行不筛——但有个附带动作：π<sub>年龄</sub>(S) 只留年龄列得 20、22、20、19，而**关系是集合，不允许有重复元素**，所以要去重 → {20, 22, 19}，**3 行 1 列**（原表 4 行）。

⚠ 这是关系代数和 SQL 最要紧的一处对不上：`SELECT 年龄 FROM S` 返回 **4 行**且**不去重**，写 `SELECT DISTINCT 年龄 FROM S` 才是 3 行、才等价于 π。这条差别常被做成叙述题的错误项。

两个下标不能装错：**σ 的下标必须是条件，π 的下标必须是列名**（也可写列号，π<sub>1,3</sub> 即第 1、3 列——真题就这么出）。顺序也不能反：要"20 岁学生的姓名"，正确写法是 **π<sub>姓名</sub>(σ<sub>年龄=20</sub>(S))**，先挑行再挑列；反过来写 σ<sub>年龄=20</sub>(π<sub>姓名</sub>(S)) 非法——π 出来的表只剩姓名一列，年龄列已被扔掉，外层拿什么去比？

### 二、两张表怎么凑一起：笛卡尔积 ×

**笛卡尔积 R×S**：左表每一行去配右表每一行，配出的每条把两边的列**全部拼起来**。

先把 S 削小一点好数：S′ = π<sub>学号,姓名</sub>(σ<sub>学号='S01'∨学号='S02'</sub>(S))，**2 行 2 列**（换小表只为让 10 行铺得下，规则一模一样，数完就换回全表）。S′ × SC：

```
S′的行｜SC 的行   学号对得上吗    S′的行｜SC 的行   学号对得上吗
S01 张三｜S01 C01 85    ✓        S02 李四｜S01 C01 85    ✗
S01 张三｜S01 C02 70    ✓        S02 李四｜S01 C02 70    ✗
S01 张三｜S02 C01 90    ✗        S02 李四｜S02 C01 90    ✓
S01 张三｜S02 C02 60    ✗        S02 李四｜S02 C02 60    ✓
S01 张三｜S03 C01 55    ✗        S02 李四｜S03 C01 55    ✗
```

**行数 = 2×5 = 10，列数 = 2+3 = 5。** 推广：R 有 m 行 a 列、S 有 n 行 b 列，则 **R×S 有 m×n 行、a+b 列**。换回全表：S×SC = 4×5 = **20 行**、3+3 = **6 列**。

10 行里只有 4 行有意义，其余 6 行全是**错配**（张三配了李四的成绩）。所以笛卡尔积几乎不单独用，它是连接的原料——SQL 里 `FROM S, SC` 忘了写条件，出来的就是它。

### 三、连接 ⋈ 就是笛卡尔积后面接一个选择

**θ 连接（读 theta，即条件连接）** R ⋈<sub>条件</sub> S：先做笛卡尔积，再用 σ 筛出满足条件的行，即 σ<sub>条件</sub>(R×S)。条件里的比较符全是等号时特称**等值连接**；等值连接再去掉重复列就是下面的自然连接——三者一层套一层，θ 最宽、自然最窄。S′ ⋈<sub>S′.学号=SC.学号</sub> SC 就是从上表留下打 ✓ 的 4 行：

```
S′.学号 姓名 SC.学号 课程号 成绩    ← 5 列，学号出现了两次
S01    张三   S01    C01   85       S02   李四   S02   C01   90
S01    张三   S01    C02   70       S02   李四   S02   C02   60
```

**学号列出现了两次**，内容完全一样。等值连接不管这件事，列数仍是 a+b = **5 列**。

**自然连接 R ⋈ S** 就是把这尾巴收拾干净的版本，规则固定成两条：① 按**两表全部同名属性**取值相等来连（不用写条件）；② 结果里**同名属性只留一份**。于是上表变成 4 行 4 列：学号、姓名、课程号、成绩。

**先别往下看：S(学号,姓名,年龄) 是 3 列，SC(学号,课程号,成绩) 也是 3 列，S ⋈ SC 有几列？**

多数人脱口而出 6。数一遍：学号、姓名、年龄、课程号、成绩，**5 列**——公共属性"学号"两边各带一份，按规则②扔掉一份。公式于是自己长出来了：

> **笛卡尔积、等值连接：列数 = a + b**（重复列全保留）
> **自然连接：列数 = a + b − 公共属性个数**

这个减号是本课上午题最高频的考点，真题反复拿它出题（练习 1、2、5 全是它）。

**行数没有公式，只能看实际匹配。** S ⋈ SC = **5 行**（赵六一门课没选，配不上任何行，直接消失）；三表连起来 S ⋈ SC ⋈ C = **5 行 6 列**（3+3+2 减掉学号、课程号两个公共属性）。**列数才有公式，行数必须数。**

（还有个除运算 ÷，回答"选修了全部课程的学生"这类问题，中级几乎不考，认脸即可。）

### 四、落单的行怎么办：外连接

自然连接把赵六**弄丢了**。他确实一门课没选，但"全校学生选课情况一览"这种报表里他不该凭空消失。

**外连接**：连接结果之外把落单的行也留下，另一边的列用 **NULL**（空值，"没有值/未知"，既不等于 0 也不等于空串）填上。三个变种：**左外连接 R ⟕ S** 保留左表全部行、**右外连接 R ⟖ S** 保留右表全部行、**全外连接 R ⟗ S** 两边落单的都保留。

**先别往下看：S 有 4 行，S 左外连接 SC 有几行？**

不是 4。匹配上的仍是 5 行（张三 2 门、李四 2 门、王五 1 门），再加赵六补 NULL 的 1 行，共 **6 行**：

```
S01 张三 20｜C01 85    S03 王五 20｜C01 55
S01 张三 20｜C02 70    S04 赵六 19｜NULL NULL  ← 落单的赵六被留下，右半边补空值
S02 李四 22｜C01 90
S02 李四 22｜C02 60
```

同一对表**右外连接只有 5 行**——SC 每一行都能在 S 里找到学生，右边一行不落单，所以这里全外连接也等于左外的 6 行。要看出三者真不同，得找一对"两边各有落单行"的表，从贯穿表里各切一块就有：甲 = π<sub>学号,姓名</sub>(σ<sub>年龄=20</sub>(S)) = {(S01,张三), (S03,王五)}，乙 = π<sub>学号,成绩</sub>(σ<sub>课程号='C02'</sub>(SC)) = {(S01,70), (S02,60)}。按学号连：**内连接**（只留两边都配上的行，也就是前面讲的等值/自然连接）**1 行**，只有 S01 两边都在；**左外 2 行**（多出 S03 王五 NULL）、**右外 2 行**（多出 S02 NULL 60）、**全外 3 行**。

SQL 写法是 `LEFT JOIN` / `RIGHT JOIN` / `FULL JOIN`，加不加 OUTER 都一样；不带方向的 `JOIN … ON` 就是上一节的 θ/等值连接，**落单行一律丢掉**。

### 五、把表当集合看：并、交、差

**前提先说，这是最常考的一句。** ∪、∩、− 要求两关系**相容**（并相容）：**属性个数相同，且对应位置的属性取自同一个域**。道理很直白——并起来的还得是张合法的表，每行列数与每列含义必须统一；把 3 列的学生表和 2 列的课程表并起来，新表第三格填什么？注意只要求个数与域一致，**列名可以不同**。

拿两个从**不同表**切出来、都只剩"学号"一列的关系演示（相容不要求同源）：A = π<sub>学号</sub>(σ<sub>课程号='C02'</sub>(SC)) = {S01, S02}（选了操作系统的），B = π<sub>学号</sub>(σ<sub>年龄=20</sub>(S)) = {S01, S03}（20 岁的）。

- **并 A∪B**：两边的行合起来，**重复的只留一份** → {S01, S02, S03}，**3 行**。SQL 的 `UNION` 去重，`UNION ALL` 不去重（同样两句得 4 行）。
- **交 A∩B**：两边都有的 → {S01}，**1 行**（SQL `INTERSECT`）。交还能用差写出来：A−(A−B) 也得 {S01}，所以交不算基本运算。
- **差 A−B**：在 A 不在 B → {S02}（选了操作系统但不满 20 岁）。**差没有交换律**：B−A = {S03}（20 岁但没选操作系统），完全是另一行（SQL `EXCEPT`）。

### 六、SQL 的执行顺序：一条队伍推出所有规则

培训资料给的语句骨架：

```
SELECT [ALL|DISTINCT] <目标列表达式>[,…]   FROM <表名或视图名>[,<表名或视图名>]
[WHERE <条件表达式>]   [GROUP BY <列名1> [HAVING <条件表达式>]]   [ORDER BY <列名2> [ASC|DESC]]
```

写的顺序 SELECT 打头，**算的顺序不是**：

```
FROM  →  WHERE →  GROUP BY →  HAVING →  SELECT →  ORDER BY
从哪来 →  删行  →   分堆    →   删堆  →   挑列  →   排序
```

对回关系代数：FROM 是 ×/连接，WHERE 是 σ，SELECT 是 π；GROUP BY、HAVING、ORDER BY 是代数之外的扩展。

两个词先说清。**聚合函数**：把一堆值压成一个值——COUNT 计数、SUM 求和、AVG 平均、MAX 最大、MIN 最小。**分组 GROUP BY**：按指定列把行分成若干堆，每堆算一次聚合，**一堆出一行**。

在 SC 上按学号分堆：S01 {85,70}、S02 {90,60}、S03 {55}，各算平均得 **77.5、75、55**，各算 COUNT 得 2、2、1。

**HAVING 就是"分组之后的那个 σ"**：σ 删行，HAVING 删**堆**。走一遍：

```sql
SELECT 学号, AVG(成绩) FROM SC GROUP BY 学号 HAVING AVG(成绩)>=75;
① FROM SC → 5 行  ② 无 WHERE 不删行  ③ 分成 3 堆  ④ 各算平均 77.5/75/55
⑤ HAVING 删掉 55 那一堆 → 结果 S01 77.5、S02 75
```

**先别往下看：把 `HAVING AVG(成绩)>=75` 换成 `WHERE 成绩>=75`，结果一样吗？**

不一样。WHERE 排在分组**前面**，它先把 70、60、55 这三**行**删掉，只剩 S01 的 85 和 S02 的 90，再分堆算平均 → **S01 85、S02 90**。同一批数据，一个算出 77.5、一个算出 85。**WHERE 删的是行、删在算平均之前；HAVING 删的是组、删在算完之后**——绝不是"写哪个都行"。

队伍一旦记住，四条规则全变成推论：**WHERE 里不能用聚合函数**——它跑的时候还没分组，AVG 无从算起，数据库直接报错；**WHERE / GROUP BY / HAVING 里不能用 SELECT 起的别名**（标准 SQL 口径，各家宽严不一，考试按此记）——别名在 SELECT 那一步才诞生，前面的步骤看不见；**ORDER BY 里可以用别名**——它排在 SELECT 之后；**SELECT 里的非聚合列必须出现在 GROUP BY 里**——一堆只出一行，堆内各行不同的列取哪个值都说不清。

两个子句混用时必须严格按队伍走：`… WHERE 成绩>=70 GROUP BY 学号 HAVING COUNT(*)>=2` ——① WHERE 删行，留下 S01 的 85、70 和 S02 的 90；② 分堆，S01 两行、S02 一行（S03 没行，不成堆）；③ HAVING 删掉只有一行的 S02 堆 → 只剩 **S01，2 门**。

**子查询**：把一条 SELECT 的结果当另一条 SELECT 的条件用，括号里那条就是子查询。两种写法要认脸：

```sql
-- IN：内层独立算出一个集合，外层看值在不在里面
SELECT 姓名 FROM S WHERE 学号 IN (SELECT 学号 FROM SC WHERE 课程号='C01');
-- EXISTS：对外层每一行跑一次内层，看"是否存在至少一行"；内层引用外层的 S.学号，叫相关子查询
SELECT 姓名 FROM S WHERE EXISTS (SELECT * FROM SC WHERE SC.学号=S.学号 AND SC.课程号='C01');
```

两句在本讲表上都得 **张三、李四、王五**。换成 **NOT EXISTS** 就是"没选数据库的学生"→ **赵六**，下午题里"没……过"的问法基本都靠它。**内层漏写关联条件 `SC.学号=S.学号`，就退化成"只要表里有任何一条数据库成绩，全体学生都命中"**。

**多表连接有两种写法，下午题都出现**：`SELECT 姓名,课程名,成绩 FROM S JOIN SC ON S.学号=SC.学号 JOIN C ON C.课程号=SC.课程号`，或老式的把连接条件塞进 WHERE：`… FROM S, SC, C WHERE S.学号=SC.学号 AND C.课程号=SC.课程号`。两句结果完全相同，都是 **5 行 3 列**（张三-数据库-85 …… 王五-数据库-55）；而**第二种漏掉 WHERE 里的连接条件就退化成笛卡尔积**（4×5×3 = 60 行垃圾）——这正是"连接 = × 后接 σ"在 SQL 里的落地。

其余零碎语法（培训资料逐条列过，认脸即可）：**SQL 关键字不区分大小写**，`select` 与 `SELECT` 等价；`AS` 起别名；`LIKE` 匹配字符串，**`%` 配任意多个字符、`_` 配任意一个字符**（`姓名 LIKE '张%'` 得张三，`LIKE '_四'` 得李四）；`ORDER BY 列 [ASC|DESC]` **默认升序，降序必须写 DESC**；`DISTINCT` 去重。

### 七、SQL 的三张面孔，以及谁能碰这些表

按管什么，SQL 语句分三类，这分类本身就是选择题：

- **DDL 数据定义**——管表的骨架：`CREATE TABLE` / `ALTER TABLE`（加列）/ `DROP TABLE`，以及 `CREATE INDEX`、`CREATE VIEW`。建表时 `PRIMARY KEY()` 指主键、`FOREIGN KEY()` 指外键，`NOT NULL` 不许为空、`UNIQUE` 不许重复。
- **DML 数据操纵**——管表里的数据：`SELECT` 查、`INSERT INTO 表 VALUES(…)` 插、`UPDATE 表 SET 列=值 WHERE…` 改、`DELETE FROM 表 WHERE…` 删。
- **DCL 数据控制**——管谁能碰：`GRANT` / `REVOKE`。

**索引 index**：给某一列另建一份排好序的目录，查这列时不必全表扫。`CREATE UNIQUE INDEX IX_S ON S(姓名)` 建唯一索引，顺带禁止姓名重复。代价是**增删改时要维护索引，反而变慢，还额外占存储**。

**视图 view**：把一条查询语句存下来当表用，它**本身不存数据**，每次查才去跑底下那条 SQL，所以又叫**虚表**。`CREATE VIEW V_优秀 AS SELECT S.学号,姓名,课程号,成绩 FROM S,SC WHERE S.学号=SC.学号 AND 成绩>=80;` 查它得 2 行：张三 C01 85、李四 C01 90。它的两个用处正是考点：**简化复杂查询**，以及**安全——只把该看的行列开放给某个用户**（薪资列不写进视图就查不到）。视图对应三级模式里的**外模式**。

**GRANT / REVOKE**：数据库多人共用，得控制"谁能对哪张表做什么"，还要能转授和收回。语句拆成几个可判定的零件：

```sql
GRANT SELECT, UPDATE ON TABLE S TO U1 WITH GRANT OPTION;
--  ↑动词  ↑权限        ↑对象     ↑被授人  ↑能不能转授
GRANT ALL PRIVILEGES ON TABLE S TO PUBLIC;   -- 把全部权限授给全体用户
REVOKE UPDATE ON TABLE S FROM U1 CASCADE;    -- 连同 U1 转授出去的一并收回
```

- **动词**：GRANT 授权、REVOKE 收回，介词跟着变——**GRANT…TO，REVOKE…FROM**。
- **权限**：SELECT / INSERT / UPDATE / DELETE，或 **ALL PRIVILEGES**（全部）；只写 SELECT 就只有查询权，不是"任何操作"。
- **对象**：表或视图。**被授人**：具体用户，或 **PUBLIC**（全体用户）。
- **WITH GRANT OPTION**：有＝允许 U1 把权限**再转授**给别人，没有＝只能自己用。它管的是能不能转授，**与权限大小无关**。
- **CASCADE**：REVOKE 时**级联收回** U1 转授出去的权限；不写它而 U1 已转授过，这次收回会被拒绝。
- 硬规则：**任何情况下都不允许循环授权**——不能把权限转授回授权链的上游。

一课到这里收口：**σ 和 π 是仅有的两把刀，× 是唯一的粘合剂，连接是它俩的组合，并交差是把表当集合看**；SQL 只是这些动作的一种写法，全部"能不能这么写"的规矩都由那条队伍决定；权限、视图、索引架在这套之上，不改变查询语义。

---

## 考试怎么考

**考点① 自然连接的属性列数（上午第一高频）。** 题目长相："关系 R(A,B,C,D) 和 S(C,D,E) 自然连接后的属性列为（ ）个"，或换成中文表问"几元关系"（部门 3 列 + 员工 5 列、公共属性部门代码 1 个 → 7 元）。常见错法：直接相加（那是笛卡尔积/等值连接的列数，正是干扰项来源）；减 1 而不是减公共属性的**个数**；把"几元关系"当行数去数。

**考点② 关系代数 ↔ SQL 互译，含把 R⋈S 展开成 π(σ(R×S))。** 给一句 SQL 问等价代数式，或给 π/σ 式让你填 SELECT / FROM / WHERE 三个空。先看**下标装的是什么**——σ 装条件、π 装列名，装反的直接排除，通常砍掉一半；再看**嵌套顺序**，σ 在里 π 在外。列号是这类题唯一的机关：**编号永远相对括号里那张表**——σ(R×S) 按 a+b 列编（R 的列在前、重复列各占一号），σ(R⋈S) 按 a+b−公共属性 列编，同一个"第 6 列"在两种基准下不是同一列（练习 1 用前者、练习 2 用后者）。剩下三个动作固定：**π 的下标 → SELECT 的列**（展开成 R×S 时就是把 S 侧的重复列号排除掉），**FROM 后是逗号分开的两张表**，**WHERE 要写全"等值条件 + 额外的 σ 条件"并用 AND 连**。常见错法：用 OR 连；给列号加引号写成 σ<sub>'2'>'7'</sub>——**加引号就成了字符串常量而不是列号**，是本题型的招牌干扰项；只写 σ 忘了外层 π。

**考点③ WHERE 与 HAVING、执行顺序。** 给一条含 WHERE + GROUP BY + HAVING 的 SQL 问结果有哪几行。照队伍走：**先删行、再分堆、再删堆**。常见错法：拿原始表数 COUNT/算 AVG，忘了 WHERE 已先删过行；以为两者可互换。

**考点④ GROUP BY 分组聚合的 SQL 填空（下午题 2 的主力）。** 一条 SQL 挖 1–3 个空，按空计分。判定套路：**带聚合函数的条件只能是 HAVING，不带就是 WHERE**；"按各种 X 分别统计"必有 `GROUP BY X` 且 X 要进 SELECT；`AS` 的格式是"表达式 AS 别名"，写反是标准错误项。常见错法：自造表名列名（题干里全给了，必须原样抄，写错一处该空不得分）；多表连接漏写连接条件；SELECT 里写了非聚合列却没进 GROUP BY。

**考点⑤ GRANT / REVOKE 与视图索引叙述题。** 权限题按零件逐个判。常见错法：把 `WITH GRANT OPTION` 当成"授予全部权限"（那是 ALL PRIVILEGES，它只管转授）；看见 `GRANT SELECT` 就选"能做任何操作"；介词写反。叙述题的错误项只有三种造法：给视图安上"存了一份数据副本"，给索引安上"越多越好/不占空间"，把并交差的相容条件改成"列名必须相同"（只要求个数相同、对应属性同域）。

---

## 练习

> 【真题】题干选项一字未改；【模拟·重现XXXX考点】＝按该场次已核实考点自编，非原题原文。

**1.【真题·年份待核实】** 给定关系 R(A, B, C, D) 和关系 S(C, D, E)，对其进行自然连接运算 R⋈S 后的属性列为（ ）个；与 σ<sub>R.B>S.E</sub>(R⋈S) 等价的关系代数表达式为（ ）。
　　第一空：A. 4　B. 5　C. 6　D. 7
　　第二空：A. σ<sub>2>7</sub>(R×S)　B. π<sub>1,2,3,4,7</sub>(σ<sub>'2'>'7'∧3=5∧4=6</sub>(R×S))　C. σ<sub>'2'>'7'</sub>(R×S)　D. π<sub>1,2,3,4,7</sub>(σ<sub>2>7∧3=5∧4=6</sub>(R×S))

**2.【真题·年份待核实】** 若有关系 R（ABCDE）和 S（BCFG），则 R 和 S 自然连结运算后的属性列有（51）个，与表达式 π<sub>1,3,6,7</sub>(σ<sub>3<6</sub>(R⋈S)) 等价的 SQL 语句如下：SELECT（52）FROM（53）WHERE（54）；
　　(51) A.5　B.6　C.7　D.9
　　(52) A. A,R.C,F,G　B. A,C,S.B,S.F　C. A,C,S.B,S.C　D. R.A,R.C,S.B,S.C
　　(53) A. R　B. S　C. RS　D. R,S
　　(54) A. R.B=S.B AND R.C = S.C AND R.C<S.B　B. R.B=S.B AND R.C = S.C AND R.C<S.F
　　　　 C. R.B=S.B OR R.C = S.C OR R.C<S.B　D. R.B=S.B OR R.C = S.C OR R.C<S.F

**3.【真题·年份待核实】** 某销售公司数据库中的零件关系 P(零件号，零件名称，供应商，供应商所在地，库存量)，函数依赖集 F={零件号→零件名称，(零件号，供应商)→库存量，供应商→供应商所在地}。零件关系 P 属于\_\_54\_\_。查询各种零件的平均库存量、最多库存量与最少库存量之间差值的 SQL 语句如下：`SELECT 零件号,__55__ FROM P __56__;`
　　54、A. 1NF　B. 2NF　C. 3NF　D. 4NF
　　55、A. AVG(库存量)AS 平均库存量，MAX(库存量)−MIN(库存量)AS 差值　B. 平均库存量 AS AVG(库存量)，差值 AS MAX(库存量)−MIN(库存量)　C. AVG 库存量 AS 平均库存量，MAX 库存量−MIN 库存量 AS 差值　D. 平均库存量 AS AVG 库存量，差值 AS MAX 库存量−MIN 库存量
　　56、A. ORDER BY 供应商　B. ORDER BY 零件号　C. GROUP BY 供应商　D. GROUP BY 零件号

**4.【模拟·重现2021上考点】** 与 SQL 语句 `SELECT 姓名 FROM S WHERE 年龄=20` 等价的关系代数表达式是（ ）。
A. σ<sub>年龄=20</sub>(π<sub>姓名</sub>(S))　B. π<sub>姓名</sub>(σ<sub>年龄=20</sub>(S))　C. π<sub>年龄=20</sub>(σ<sub>姓名</sub>(S))　D. σ<sub>姓名</sub>(π<sub>年龄=20</sub>(S))

**5.【模拟·重现2022下考点】** 关系 R 有 4 个属性，关系 S 有 3 个属性，两者有 2 个同名属性。R ⋈ S（自然连接）的结果有多少个属性？A. 7　B. 5　C. 6　D. 4

**6.【模拟·重现2024上考点】** SQL 语句 `GRANT SELECT ON TABLE Student TO U1 WITH GRANT OPTION` 的含义是（ ）。
A. U1 只能查询 Student 表，不能将该权限授予其他用户
B. U1 能查询 Student 表，并可以将该查询权限授予其他用户
C. U1 能对 Student 表做任何操作
D. 收回 U1 对 Student 表的查询权限

**自测**：
7.【模拟】R 有 3 行 2 列、S 有 4 行 3 列且无同名属性。R×S 和 R⋈S 各多少行多少列？
8.【模拟】本讲 SC 表上执行 `SELECT 课程号, AVG(成绩) FROM SC GROUP BY 课程号 HAVING AVG(成绩)>70`，结果是哪几行？
9.【模拟】把 `SELECT DISTINCT 课程号 FROM SC WHERE 成绩>=85` 写成关系代数表达式，并在本讲表上求值。
10.【模拟】"查每个学生的选课门数，只保留选了 2 门及以上的"，空格填 HAVING 还是 WHERE？为什么另一个不行？
11.【模拟】执行 `GRANT SELECT ON TABLE S TO U1;`（无 WITH GRANT OPTION）后，U1 能不能把查询权再授给 U2？若 U1 有该选项且已授给 U2，管理员执行 `REVOKE SELECT ON TABLE S FROM U1;` 会怎样？

### 参考答案

**1. B、D**。第一空：公共属性 C、D 共 2 个，4+3−2 = **5**。第二空：R×S 的 7 列从 1 编号 → 1=A、2=B、3=R.C、4=R.D、5=S.C、6=S.D、7=E。等值条件 R.C=S.C、R.D=S.D 即 3=5 ∧ 4=6；去重复列靠外层 π 只取 1,2,3,4,7（排除 S 侧的 5、6）；附加条件 R.B>S.E 即 2>7，合起来正是 D。A、C 缺外层 π；B 给列号加了引号，**'2'>'7' 比的是两个字符串常量而不是列**，与 D 只差这对引号。

**2. C、A、D、B**。(51) 公共属性 B、C 共 2 个，5+4−2 = **7**。(52) 七列按 A B C D E F G 编号，π<sub>1,3,6,7</sub> 取 **A、C、F、G**，写成 `A,R.C,F,G`（C 两表都有必须加前缀）。(53) 查两张表，写 `R,S`。(54) WHERE 要用 **AND** 连两部分：等值条件 `R.B=S.B AND R.C=S.C`，加上 σ<sub>3<6</sub> 即第 3 列 C 小于第 6 列 F 得 `R.C<S.F`。用 OR 的 C、D 一眼排除；A 把第 6 列错当成 S.B。

**3. A、A、D**。54 属讲义 31 的考点：主键是 (零件号,供应商)，而"零件号→零件名称"只用半个码，存在部分函数依赖，卡在 **1NF**。55 语法是"表达式 AS 别名"，B、D 把两边写反，C 的 `AVG 库存量` 漏括号。56 要"各种零件"分别统计就按零件号分组；ORDER BY 只排序不分组，不分组时聚合函数会把整表压成一行。

**4. B**。C、D 的下标装反，直接排除；A 顺序反了，先投影就把年龄列扔了，外层无从比较。

**5. B**。自然连接去重复列：4+3−2 = **5**；干扰项 7 是笛卡尔积/等值连接的列数。

**6. B**。动词 GRANT 排除 D；权限只有 SELECT 而非 ALL PRIVILEGES，排除 C；带 WITH GRANT OPTION 可转授，排除 A。

**7.** R×S = 3×4 = **12 行**、2+3 = **5 列**。R⋈S 也是 **12 行 5 列**——**没有公共属性时等值条件是空的，谁都跟谁匹配，自然连接退化成笛卡尔积**，减号那项减 0。

**8.** 只有 **C01 一行，平均 76.67**（(85+90+55)/3）；C02 平均 65，被 HAVING 整组删掉。

**9. π<sub>课程号</sub>(σ<sub>成绩>=85</sub>(SC))**。成绩≥85 的是 S01-C01-85、S02-C01-90，投影出课程号得 C01、C01，π 去重 → **{C01}，1 行**，正对应 DISTINCT。

**10.** 填 **HAVING**（`… GROUP BY 学号 HAVING COUNT(*)>=2`）。条件里有聚合函数 COUNT(*)，而 WHERE 排在 GROUP BY 之前，那时还没分堆、COUNT 无从算起；按聚合值筛掉整个"堆"正是 HAVING 的职责。本讲表上得张三、李四。

**11.** 不能——没有 `WITH GRANT OPTION` 只能自己用，无权转授。第二问：U1 已转授给 U2，不加 CASCADE 的 REVOKE 会被拒绝（U2 那份会成无源之水）；要连 U2 一起收得写 `… FROM U1 CASCADE;`。

---

## 速记

- σ 挑行＝WHERE，π 挑列＝SELECT；**σ 下标是条件、π 下标是列名，先 σ 后 π**；π 自动去重＝`SELECT DISTINCT`
- **笛卡尔积：m×n 行、a+b 列**；等值连接列数还是 a+b（重复列保留）；**自然连接 = a+b−公共属性个数**；**行数没有公式，只能数**
- 无公共属性时自然连接退化成笛卡尔积；外连接把落单行留下、对面补 NULL，左外/右外/全外分别保左表/右表/两边
- 并交差要求**属性个数相同、对应属性同域**；并去重；**差没有交换律**；A∩B = A−(A−B)
- 执行顺序 **FROM→WHERE→GROUP BY→HAVING→SELECT→ORDER BY**：从哪来→删行→分堆→删堆→挑列→排序
- **WHERE 删行在算之前、HAVING 删组在算之后**；WHERE 不认聚合也不认别名，ORDER BY 认别名；SELECT 的非聚合列必须进 GROUP BY；IN 内层独立算完给外层用，EXISTS 逐行判有无且内层必须写关联条件
- DDL 定义 / DML 操纵 / DCL 控制；视图是**虚表不存数据**、对应外模式；索引**快查询、慢增删改、占空间**
- **GRANT…TO / REVOKE…FROM**；**WITH GRANT OPTION = 可转授**、ALL PRIVILEGES = 全部权限、PUBLIC = 全体用户、CASCADE = 级联收回；不许循环授权
