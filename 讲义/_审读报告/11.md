# 11 审读报告 · 2026-09-16

## 结论

能一遍读懂。六节主线顺、六节都能复述，五个推演块跳过后主线不缺东西。最大的一个问题在第五节收口：正文写「两者边集相同、总权相同」，读起来像一般规律，速记却说「形状不一定相同」，而题库第 2 题（真题）第二空的 B 项正是这个坑；同一段还从头到尾没说 Prim/Kruskal 是「贪心法」，本节「考试这样问」和题库 2 第一空都在问。

## 卡壳点（按出现顺序）

| # | 位置（节 + 段首几个字） | 卡在哪 | 类型 | 严重度 | 建议改法 |
|---|---|---|---|---|---|
| 1 | 第一节「有向图里要分开算」 | 「弧」「尾」「头」首现无解释；题库 1、10 题干都用「弧」 | 术语 | 🟡 | 「边带箭头的叫有向图」后加半句：「有向图的边叫弧，箭尾是起点、箭头是终点」 |
| 2 | 第一节两处引用「第六节那张有向图」 | 读者此刻没见过那张图，只能先信 | 顺序 | 🟡 | 把第六节那行 8 条定向边的清单提前贴一行；或把两个数字例子挪到第六节末尾回收 |
| 3 | 开讲主图 + 第二节「办法一：邻接矩阵」 | 规则说「是网就填权值、无边填 ∞」，算例却填 0/1，对不上 | 例子 | 🟡 | 矩阵前加一句「权值第四节才用，这里先当普通无向图、只填有没有边」 |
| 4 | 第三节 BFS 过程 | 「访问 = 出队」口径下，C 在队里未出队算不算已访问？实际口径「入队即标记」正文没说 | 链条 | 🟡 | BFS 定义句后加：「入队的同时就打标记，防止同一个点被塞进队列两次」 |
| 5 | 第三节「拿树来对照」 | 「前序遍历」「层次遍历」首现无解释；段末又塞「BFS 只保证边数最少」 | 术语 / 段落 | 🟢 | 括号各加五个字；最后一句另起一段 |
| 6 | 第四节结果段 | 表格只有 dist 没有前驱，路径怎么从表里读出来没教 | 脱节 | 🟡 | 表后加一句「路径倒着读：谁最后一次改小了 F，F 的前一站就是谁……倒过来就是 A→C→B→D→F」 |
| 7 | 第四节推演「穷举验算」 | 列了四条就「……」，不知道是不是真穷举完 | 说过头 | 🟢 | 「……」换成「其余走法都要经过 D–E(8) 或 A–B(6)，只会更长」，或标题改「抽查几条走法」 |
| 8 | 第五节「两个算法的区别就在这里」 | 「两者边集相同」像一般规律，速记却说「形状不一定相同」，正文没说「有等权边时可能挑出不同的树」；题库 2（真题）第二空 B 项就是这个坑 | 说过头 / 脱节 | 🔴 | 改成「本图两者边集相同，是因为八条边权互不相同、最小生成树只有一棵；一般情况下总权一定相同，形状不一定相同」 |
| 9 | 第五节「各自适合谁」 | 正文没说 Prim、Kruskal 是贪心法，只给 Dijkstra 贴过；题库 2 第一空在问 | 脱节 | 🟡 | 前加一句：「两个算法和 Dijkstra 一样，每一步只挑当前最小的那条边、定了不回头，同属贪心法」 |
| 10 | 第五节「各自适合谁」 | 「扫一遍」扫的是什么没说，程序员会想成扫横跨边得 O(n·e) | 链条 | 🟢 | 改成「扫一遍树外的全部顶点（每个顶点记着自己到树的最小距离）」 |
| 11 | 第五节推演「穷举验算最小生成树是否唯一」 | 只给 30 棵、次小 17 的结论没过程 | 段落 | 🟢 | 至少列出次小 17 那棵是哪五条边 |
| 12 | 第六节「把上面那张图的八条边原样定向」 | 无向边本无「原样」；定向后只给边表没给带箭头的图 | 图 | 🟢 | 删「原样」；边表前加一句「C→B、E→D 两条向左，其余都向下」，或复制主图加箭头（需拍板） |
| 13 | 第六节「拓扑序列不唯一」 + 速记 | 正文当一般结论写，速记写「通常不唯一」，口径不一 | 说过头 | 🟢 | 正文改「拓扑序列不一定唯一」 |
| 14 | 定位 vs 收口段 | 定位说「四个问题」，收口说「只换过三次问题」 | 段落 | 🟢 | 收口改成四问 |

第一个例子：主图一出场带权，但前三节只用顶点和边不碰权，权值是惰性标签，不算同时压两个概念；之后每节只加一个维度（带权 → 定向），合规。
图的四问：主图、矩阵、邻接表、Dijkstra 表、三段过程，标题图正文一致、刻度可定位；唯一缺口是第六节有向版本没有图（#12）。

## 每节复述
六节均能复述，无 ✗。（第一节：顶点边度、握手定理、连通/强连通、完全图；第二节：矩阵 n² 与邻接表 n+2e、稠密稀疏；第三节：打标记、DFS 栈 BFS 队列；第四节：Dijkstra 冻结与松弛、贪心单源非负、Floyd；第五节：生成树 n−1、Kruskal 捡边 Prim 长树、稠密 Prim 稀疏 Kruskal；第六节：AOV、入度 0 先输出、不一定唯一、排不完有环。）

## 题目反做
12 题中 10 题能只用正文做；第 2 题第一空勉强（贪心要自己类推，#9）、第二空不稳（B 项排除不掉，#8）；第 7 题路径换图就要靠「本轮松弛」列倒推，正文没教（#6）。
题库考点①②③④⑥ 都能回正文找到；⑤ 的「贪心法」「形状不一定相同」正文找不到（#8、#9）。

## 不需要改的地方
三处「先别往下看」都放在真会猜错的位置；连通分量用「拆掉 A 的两条边」演示、第三节回扣；Kruskal 与 Prim 的区别用 D-F(2) 一条边讲透；Dijkstra 表每轮加粗改动格、表后补「每轮挑法」；拓扑每步写剩余入度；五个推演块都真的可跳过；六节一张图、一次只加一个维度，骨架不要重排。
