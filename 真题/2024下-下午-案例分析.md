# 软件设计师 2024年下半年 下午试题及答案解析

> 来源：
> - 【主源】CSDN·柒柒幸运星《2024下半年软件设计师真题（考生回忆版）》“二.应用技术”部分 https://blog.csdn.net/scx_yatoy/article/details/144409718 （收录 5 道题，附博主重绘的用例图、类图、E-R 图和答案图）
> - CSDN·qq_31066637《2024年下半年软件设计师上午真题【回忆】》“案例题”部分 https://blog.csdn.net/qq_31066637/article/details/143660654 （只记了算法题问题 2 和 Java 题五个空的答案）
>
> 转写说明：网页原文用 curl 抓取后去标签逐字转写。题图取自主源博主的重绘图（考后凭记忆画的，不是原卷截图），看图转成文字，标“按来源重绘图转写”。原图没有的标“原图缺”。只改了明显错字（如“交工工具管理”→“交通工具管理”）。伴读的核对与补充一律写在【转写注】里，不混进来源答案。
>
> 本卷说明：考后回忆整理版（机考，2024 年 11 月 10 日，只有一个批次）。回忆很不完整：
> - **题号是伴读按下午卷惯例题型顺序排的**（试题一数据流图、试题二数据库、试题三 UML、试题四 C 算法、试题五 C++、试题六 Java），主源自己的编号是“1.UML 类图、2.算法题、3.数据流图、4.E-R 图、6.JAVA 代码题”，原卷题号未经证实。
> - 试题一（数据流图）：说明只剩一句概括和外部实体名单，上下文图、0 层图原图缺。
> - 试题二（E-R 图）：说明与需求描述缺失；实体联系图、关系模式按来源重绘图转写。
> - 试题三（UML）：说明只剩功能列表；用例图、类图按来源重绘图转写；问题 2 的 C5 答案缺失。
> - 试题四（C 算法）：**代码不是原题**，来源博主注明“具体代码记不清了，就自己写的代码，跟原题中的空对应上了”。
> - 试题五（C++）：各来源均未收录，整题缺失。
> - 试题六（Java）：说明缺失；**代码不是原题**，来源博主注明“由于源代码缺失，以下代码是根据答案自己写出来的”。
> - 只有试题四问题 2 和试题六能与第二个来源对照；其余各题只有主源一个来源。存疑处见各题【转写注】。

---

## 试题一（共 15 分）

【说明】
相机的构成、拍照及存储功能描述；【缺失输入输出图示】

相机构成组件及涉及的外部设备包括：镜头、对焦开关、手动调焦环、自动调焦传感器、亮度传感器、闪光灯、拍摄按钮、模式切换按钮、电脑、显示屏；

（原卷说明的其余文字，以及上下文数据流图、0 层数据流图，原图缺。）

【问题 1】（5 分）
根据说明的词语，给出 E1～E10；

【问题 2】（2 分）
根据说明的词语，给出 D1～D2；

【问题 3】（6 分）
根据说明的词语，给出‘可视化数据’、‘媒体流’、‘闪光灯数据流’的数据流组成；

【问题 4】（2 分）
描述相机拍摄过程的结构化设计步骤；

### 参考答案与解析

**问题 1**

（大概是这些，具体顺序记不清了）镜头、对焦开关、手动调焦环、自动调焦传感器、亮度传感器、闪光灯、拍摄按钮、模式切换按钮、电脑、显示屏。

【转写注】原图缺，E1～E10 与各实体的一一对应无法确定，来源只给了 10 个实体的名单。

**问题 2**

（D1 存储视频，D2 存储照片）
D1：AVI 格式文件
D2：JPG 格式文件

**问题 3**

- 可视化数据：照片 + 视频
- 媒体流：照片 + 视频
- 闪光灯数据流：传感器值 + 闪光灯功率值

**问题 4**

【答案存疑】体系结构设计、数据设计、接口设计、过程设计；

【转写注】来源自标“答案存疑”。按《软件设计师教程》，结构化设计的内容包括体系结构设计、接口设计、数据设计和过程设计，与来源答案一致，但原题问法已不可考。

---

## 试题二（共 15 分）

阅读下列说明，回答问题 1 至问题 3，将解答填入对应栏内。

【说明】
描述信息缺失。

【需求描述】
描述信息缺失。

【概念模型设计】
根据需求阶段收集的信息，设计实体联系图（不完整），如下图所示：

【图 实体联系图（不完整），按来源重绘图转写】
- 实体“货物”：属性 货物编号（下划线，主键）、名称
- 实体“列车”：属性 班列编号（下划线，主键）、名称
- 实体“客户”：属性 客户手机号码（下划线，主键）、姓名
- 联系“托运”：连接 货物（*）与 列车（*）
- 联系“订单”：图中只画出菱形，未与任何实体连线，也未画属性

【逻辑结构设计】
根据概念模型设计阶段完成的实体联系图，得出如下关系模型（不完整）：
- 货物（货物编号，名称）
- 列车（班列编号，名称）
- 客户（客户手机号码，姓名）
- 托运（货物编号，（a），时间）
- 订单（订单编号，货物编号，（b），客户手机号码，日期，数量）

【问题 1】（6 分）
根据问题描述，补充实体联系图；

【问题 2】（4 分）
补充逻辑结构设计中的 (a) 和 (b) 两处空缺及完整性的约束关系；

【问题 3】（5 分）
增设‘签证’实体，包含属性：签证编号，签证国别和日期，补充客户与签证之间的委托联系并给出签证关系模式；

### 参考答案与解析

**问题 1**

【答案图，按来源重绘图转写】在原图基础上补充：
- 联系“订单”连接 货物（*）、列车（*）、客户（1）；
- 联系“订单”的属性：订单编号（下划线）、日期、数量。

**问题 2**

（a）班列编号；完整性约束为：主键：（班列编号，货物编号）；外键：班列编号，货物编号
（b）班列编号；完整性约束为：主键：订单编号；外键：班列编号，货物编号，客户手机号码

【转写注】（伴读核对）托运是货物与列车之间的多对多联系，转换为关系模式时取两端实体的主键组合为主键，所以托运的主键是（货物编号，班列编号），两者分别是参照货物、列车的外键；订单关系中货物编号、班列编号、客户手机号码分别参照三个实体，均为外键，与来源答案一致。

**问题 3**

【答案图，按来源重绘图转写】在问题 1 答案图基础上增加：实体“签证”，属性 签证编号（下划线）、签证国别、日期；联系“委托”连接 客户（1）与 签证（*）。

签证（签证编号，签证国别，日期，客户手机号码）
主键：签证编号；
外键：客户手机号码；

【转写注】（伴读核对）客户与签证是 1 对多联系，把“1”端（客户）的主键放进“多”端（签证）关系中作外键即可，不必单独建委托关系，与来源答案一致。

---

## 试题三（共 15 分）

【说明】
涉及功能：交通工具管理、信号灯配时、交通实时数据统计、交通拥堵预测、班次调整、交通应急管理等；

（原卷说明的其余文字缺失。）

【图 1-1 用例图，按来源重绘图转写】
- 参与者 A1、A2、A3；图左上角另有文字“传感器”，连线指向 U1。
- A1 与 U1、U2、U3 关联。
- U2 → U1，标注 extend；U3 → U1，标注 extend。
- A2 与 U4 关联；A3 与 U4 关联。
- A3 与用例“导出事件信息”关联；“导出事件信息”→ U5，标注 extend。

【图 1-2 类图，按来源重绘图转写】
- C1 → C2（带箭头的关联）；C2 → “交通信号灯”；C2 → C4。
- Bus、Subway 都以空心三角箭头指向 C3（泛化，C3 为父类）。
- C3 与 C4 之间为聚合，空心菱形在 C4 一端。
- C5 与 C4 之间为聚合，空心菱形在 C4 一端。
- C5 → “交通事故相关”。

【问题 1】（8 分）
根据说明中的描述，给出图 1-1 用例图中 A1～A3 所对应的参与者名称和 U1～U5 处所对应的用例名称；

【问题 2】（5 分）
根据说明中的描述，给出图 1-2 类图中 C1～C5 所对应的类名；

【问题 3】（2 分）
现有新的需求，根据事件的严重程度和优先级，逐步提交给适合的应急部门进行处理，应采用什么设计模式？

### 参考答案与解析

**问题 1**

A1：交警
A2：公交调度员
A3：相关应急部门
U1：收集交通数据
U2：优化交通信号灯的配时
U3：预测交通拥堵
U4：调整公交班次
U5：给出应急方案

**问题 2**

C1：传感器 Sensor
C2：交通数据分析 TrafficMonitor
C3：公共交通工具 Transport
C4：交通应急管理 TransportManager
C5：（缺失）

**问题 3**

【有人说是策略模式，我写的是责任链模式】
应采用责任链模式。
因为该模式的意图是使多个对象都有机会处理请求，从而避免请求的发送者和接收者之间的耦合关系，将这些对象连成一条链，并沿着这条链传递该请求，直到有一个对象处理它为止；

【转写注】（伴读核对）题干要求请求按严重程度和优先级逐级传递，直到交给合适的部门处理，符合责任链模式的意图；策略模式是在多个可互换的算法之间选择一个，与“逐级提交”不符。伴读同意责任链模式。

---

## 试题四（共 15 分）

【说明】
初始化：二叉排序树可以是一棵空树，或者是一棵具有特定结构的二叉树。其特性是，若左子树非空，则左子树上所有结点的值均小于根节点的值；若右子树非空，则右子树上所有结点的值均大于根节点的值。

插入操作：插入操作是二叉排序树构建的关键步骤。当树中不存在关键字值等于给定值的结点时，需要进行插入。插入过程如下：若原二叉排序树为空，则直接插入结点；否则，若关键字小于根节点值，则插入左子树；若关键字大于根节点值，则插入右子树。插入的结点通常是一个新添加的叶节点，位于查找失败时的查找路径上的最后一个结点的左孩子或右孩子。

查找操作：查找操作是从根节点开始，沿某个分支逐层向下比较的过程。若二叉排序树非空，先将给定值与根节点的关键字比较；若相等，则查找成功；若不相等，则根据比较结果继续在左子树或右子树上进行查找。

【C 代码】
**（来源注明：“我记得原题中代码部分包括构建和查找操作，但是具体代码记不清了，就自己写的代码，跟原题中的空对应上了”——以下代码不是原题。）**

```c
#include"stdio.h"
#include"malloc.h"
typedef struct Node
{
    int data;
    struct Node *lchild;
    struct Node *rchild;
}Node;
void InsertBst( Node *BST, int key)
{
    Node *s, *p, *q;
    p = BST;
    s = (Node *)malloc( sizeof(Node) );
    s->data = key;
    s->lchild = NULL;
    s->rchild = NULL;
    if (p == NULL){
        (1)
    }else{
        q = p;
        while( q != NULL ){
            if( s->data == q->data ){
                (2)
            }
            p = q;
            if( s->data < p->data ){
                (3)
            }else if( s->data > q->data ){
                q = q->rchild;
            }
        }
        if( q == NULL && s->data < p->data ){
            p->lchild = s;
        }else if( q == NULL && s->data > p->data ){
            (4)
        }
    }
}
int CreateBst()
{
    int keys[] = { 18, 3, 7, 20, 14, 19, 27, 2 };
    Node *BST = ( Node * )malloc( sizeof( Node ) );
    for (int i = 0; i < sizeof(keys); i++){
        (5)
    }
    return 0;
}
```

【问题 1】（10 分）
根据说明和 C 代码，填充 C 代码中的空 (1)～(5)；

【问题 2】（2 分）
n 个结点组成的二叉树最大高度是？

【问题 3】（3 分）
根据给定数据序列：36，26，41，12，38，44，6，15，68，51，构成的二叉排序树为（）；

### 参考答案与解析

**问题 1**

（1）p = s;
（2）break;
（3）q = q->lchild;
（4）p->rchild = s;
（5）InsertBst(BST, keys[i]);

【转写注】代码与空位是来源博主自拟的，这组答案只对应上面这段代码，原题的写法不可考。这段自拟代码本身有缺陷（BST 按值传入，空树时 `p = s` 改不到调用者；`sizeof(keys)` 是字节数而不是元素个数），刷题时只看思路即可。

**问题 2**

n

【转写注】qq_31066637 同样记作“n”。每个结点只有一个孩子时，二叉树退化成一条链，高度等于结点数 n。

**问题 3**

【答案图，按来源重绘图转写】
- 根 36；36 的左孩子 26，右孩子 41；
- 26 的左孩子 12；12 的左孩子 6，右孩子 15；
- 41 的左孩子 38，右孩子 44；
- 44 的右孩子 68；68 的左孩子 51。

【转写注】校验（伴读按序列逐个插入复算）：36 为根；26<36 放左；41>36 放右；12<36、<26 放 26 左；38>36、<41 放 41 左；44>36、>41 放 41 右；6 沿 36→26→12 放 12 左；15 沿 36→26→12 放 12 右；68 沿 36→41→44 放 44 右；51 沿 36→41→44→68 放 68 左。与答案图一致。

---

## 试题五（共 15 分）

（C++ 程序设计题，各来源均未收录，整题缺失。）

---

## 试题六（共 15 分）

【说明】
描述信息缺失。

【Java 代码】
**（来源注明：“由于源代码缺失，以下代码是根据答案自己写出来的，可能与源代码存在些差异。”——以下代码不是原题。）**

```java
import java.util.*;
class GraphicElement {
    //代码略...
}
abstract class Graphic {
    Protected (1);
    (2);
    (3);
    public void printName() {System.out.println(name);}
}
class LeafGraphic extends Graphic {
    public LeafGraphic(String name){
        this.name = name;
    }
    public void draw(){//代码略...}
    public void move(int dx, int dy){//代码略...}
}
class CompositeGraphic extends Graphic {
    private (4);
    public CompositeGraphic(String name){
        this.name = name;
        this.elements = new ArrayList<>();
    }
    public void draw(){//代码略...}
    public void move(int dx, int dy){//代码略...}
    public void addElement(GraphicElement element){
        this.elements.add(element);
    }
}
public class GraphicElementTest {
    public static void main(String[] args) {
        GraphicElement element = new GraphicElement();
        (5);
        graphic.addElement(element);
        //代码略...
    }
}
```

【问题】
请填写（1）～（5）缺失的代码；

### 参考答案与解析

（1）String name
（2）abstract void draw()
（3）abstract void move(int dx, int dy)
（4）List<GraphicElement> elements（这里我填的是 ArrayList，不确定给不给分）
（5）CompositeGraphic graphic = new CompositeGraphic("CompositeGraphic")

【转写注】qq_31066637 记的五个空为：String name；abstract void draw()；abstract void move(int dx, int dy)；List elements；CompositeGraphic graphic = new CompositeGraphic("Graphic")。两源一致（第（5）空构造参数字符串不同，不影响答案）。代码为来源自拟，“Protected”应为小写 protected。本题考的是组合模式：LeafGraphic 为叶子，CompositeGraphic 为组合对象，抽象类 Graphic 声明共同接口 draw()、move()。第（5）空变量必须声明为 CompositeGraphic 类型，下一行才能调用 addElement()。
